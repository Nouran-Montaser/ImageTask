<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!--Here we are defining a FileProvider path to store the camera images in a cached location instead of storing them in gallery.-->
    <external-cache-path
        name="cache"
        path="camera" />

</paths>